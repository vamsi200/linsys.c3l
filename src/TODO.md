- Centralized proc parser layer
- split_ws() helper??
- Redesign cpu_percent()
- Remove internal sleeping and use CPU snapshots
- Cache values like CLK_TCK and boot time instead of rereading procfs repeatedly.
- Centralize /proc/<pid>/stat parsing
- Centralize proc path generation
- Replace magic octal flags
- Internal parser utilities module
- IPv6 support
- UNIX socket support
- Optimize children()
- Process snapshot system
- Reduce allocations
- Process iteration API
